#include <stdio.h>
#include <stdlib.h>
#include <string.h>

//#include "Logger.h"

#define LOG(x) printf("%d",x);

int main(int argc, char const *argv[])
{
    int *p = malloc(sizeof(int));
    return 0;
}